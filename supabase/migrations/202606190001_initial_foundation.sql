-- This schema already exists in the transferred Supabase project.
-- Keep this migration as a no-op so Supabase GitHub integration can
-- record the version without re-creating existing tables/types.
select 1;
